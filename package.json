{
  "name": "citysim",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:gpu": "node tools/gpu-smoke.mjs",
    "test:deploy": "node tools/deploy-layout-test.mjs",
    "test:frustum": "node tools/frustum-test.mjs",
    "test": "npm run typecheck && npm run test:assets && npm run test:frustum && npm run test:gpu && npm run test:deploy",
    "test:assets": "node tools/asset-test.mjs",
    "assets:sheet": "node tools/asset-sheet.mjs"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "playwright": "^1.49.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
