{
  "name": "black-meridian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BLACK MERIDIAN — a single-player tactical operator game",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173",
    "shot": "node tools/screenshot.mjs",
    "audit": "node tools/audit.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "playwright": "^1.50.0",
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  }
}
